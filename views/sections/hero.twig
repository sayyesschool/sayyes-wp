<section class="hero">
	<div class="hero__content">
		<h1 class="hero__title">
			{% block title %}
				{{title}}
			{% endblock %}
		</h1>

		<div class="hero__description">
			{% block description %}
				{{description}}
			{% endblock %}
		</div>

		{% block content %}
			{{content}}
		{% endblock %}
	</div>

	{% if image %}
		<img class="hero__image" src="{{theme.link}}/static/images/mainscreens/{{image}}" alt="Команда мечты">
	{% endif %}
</section>