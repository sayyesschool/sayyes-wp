{% extends 'layouts/full-width.twig' %}

{% block head %}
<script>
    fbq('track', 'Lead');
</script>
{% endblock %}

{% block main %}
<h1 class="title page-title">{{ page.h1 ? page.h1 : page.title }}</h1>

{{ page.content }}
{% endblock main %}