<div class="subnav">
	<ul class="subnav__list">
		{% for item in items %}
			<li class="subnav__item">
				<a class="subnav__link" href="{{item.link}}"{% if item.is_external %} target="_blank"{% endif %}>{{item.title}}</a>

				{% if item.children %}
					<div class="subnav">
						<ul class="subnav__list">
							{% for item in item.children %}
								<li class="subnav__item">
									<a class="subnav__link" href="{{item.link}}"{% if item.is_external %} target="_blank"{% endif %}>{{item.title}}</a>
								</li>
							{% endfor %}
						</ul>
					</div>
				{% endif %}
			</li>
		{% endfor %}
	</ul>
</div>