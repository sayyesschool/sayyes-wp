<div class="accordion">
	{% for item in items %}
		<div class="accordion-item {{item.status}}">
			<button class="accordion-item__title">{{item.title}}</button>
			<div class="accordion-item__content">{{item.description}}</div>
		</div>
	{% endfor %}
</div>